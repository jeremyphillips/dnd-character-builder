// import { useCharacterBuilder } from '@/characterBuilder'

const DetailsStep = () => {
  // const { setDetails } = useCharacterBuilder()

  return (
    <>
      <h2>Choose details</h2>
      {/* {details.map(e => (
        <button key={e.id} onClick={() => setDetails(e.id)}>
          {e.name}
        </button>
      ))} */}
    </>
  )
}

export default DetailsStep
